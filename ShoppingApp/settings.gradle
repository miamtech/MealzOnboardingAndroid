pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        /**
        * (Step 1): Add the dependency resolution config
        * https://miamtech.github.io/mealz-documentation/docs/android/overview/installation
        */
        maven {
            url = uri("https://github.com/miamtech/releaseMealz/raw/main")
        }
    }
}
rootProject.name = "Shopping App"
include ':app'
